{
  "name": "askd-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "askd local bridge — read-only reading assistant proxy to local Claude Code / Codex CLIs. Binds 127.0.0.1 only.",
  "type": "module",
  "bin": {
    "askd-bridge": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.185"
  }
}
